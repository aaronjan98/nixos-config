# Session — 2026-04-18

- Investigated tmux cursor inconsistency in Ghostty.
- Cause: `modules/tmux.nix` set prompt/message styling but did not set tmux `cursor-colour` or `prompt-cursor-colour`, so tmux command-prompt mode could fall back to a non-red cursor path.
- Added `theme.tmux.cursor = "#FF2800"` in `themes/limerence.nix`.
- Updated `modules/tmux.nix` to set both `cursor-colour` and `prompt-cursor-colour` from the theme and enabled `ccolour,cstyle` terminal features for `xterm-ghostty` and `ghostty`.
