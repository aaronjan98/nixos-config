# 2026-06-09

## Custom OCR speed tuning

- User tested `Super+X` custom OCR on a paragraph with a simple display equation.
- Output was close, but latency was still noticeable.
- Latest attempt showed the bottleneck was not Tesseract or preprocessing: Tesseract took about 0.6s and preprocessing about 0.4s, while pix2tex spent 20s timing out on the simple display equation `dy = f'(x) Ax.`.
- Updated `ocr-custom-split` auto mode so simple display math is also skipped and rendered through the cleaned Tesseract fallback instead of waiting for pix2tex.
- Preserved display-equation punctuation so `dy = f'(x) Ax.` renders as `$$dy = f'(x) \Delta x.$$`.
- Validation on the same screenshot produced the expected display equation and both math spans were `skipped-simple`, with no pix2tex calls.

## Custom OCR theorem/fraction cleanup

- User tested a theorem paragraph containing `$b=1, c=0$`, `$f(x)=x$`, `$f'(x)=1$`, and a display equation `\frac{dx}{dx} = 1, \quad dx = dx`.
- Tesseract misread `c` as `¢`, merged `1; i.e.,` into `1;ie.,`, and split the stacked fraction into separate lines: `d -`, `LE 1, dx = dx.`, `dx`.
- Added math-token normalization so `¢`/`©` are treated as likely `c` inside math spans.
- Added suffix splitting so `f'(x) = 1;ie.,` renders as `$f'(x) = 1$; i.e.,` instead of sending the prose suffix into pix2tex.
- Added a narrow postprocessor for the common Tesseract `dx/dx` stacked-fraction failure; it collapses the three-line pattern into `$$\frac{dx}{dx} = 1, \quad dx = dx$$`.
- Validation on the same screenshot produced:
  `If in Theorem 1 we put $b = 1, c = 0$ ... function $f(x) = x$ is $f'(x) = 1$; i.e., $$\frac{dx}{dx} = 1, \quad dx = dx$$`.
- Follow-up live retest produced a slightly different Tesseract pattern: `f(x) = 1; ie,` and `Lo 1, dx = dx.`. Added coverage for that variant, including restoring `$f'(x) = 1$; i.e.,` in the identity-derivative context and accepting `Lo`/`l0`/`10` as the fraction middle-line artifact.

## Custom OCR constant-function cleanup

- User tested a constant-derivative paragraph: `$b = 0$`, `$bx + c$`, constant `$c$`, `$f(x) = c$`, `$f'(x) = 0$`, and display `\frac{dc}{dx} = 0, \quad dc = 0.`.
- Tesseract output was close but had predictable errors: `¢` for `c`, too-narrow span `+ c` instead of `bx + c`, display `f'(x) = 0; ie.,`, and stacked-fraction lines `dc`, `— =0, de = 0.`, `dx %`.
- Added known compound variables (`bx`, `dx`, `dy`, `dc`) so adjacent math spans can include them.
- Prevented lines containing `i.e.` from being treated as display math.
- Added a generalized stacked derivative collapse for `d<var>` over `dx` with a middle line like `— =0, de = 0.`; it now emits `$$\frac{dc}{dx} = 0, \quad dc = 0.$$`.
- Validation on the same screenshot produced the intended structure with all spans `skipped-simple` and no pix2tex calls.

## Custom OCR display-block routing checkpoint

- User provided a multi-line align-style display equation where Tesseract destroyed the structure (`Ay c_Au 9 cAu`, `Ax Ax AX`) and local pix2tex timed out on a partial crop.
- Implemented first structural display-block checkpoint in `ocr-custom-split`: detect math-heavy centered line groups before inline span routing, crop them as whole regions, and save `display-blocks.json` plus `display-crops/block-*.png`.
- Display blocks are excluded from normal inline span routing, so Tesseract no longer gets to fragment each internal display line into mixed prose/math spans.
- `debug-overlay.png` now draws display blocks in cyan and inline spans in orange/red.
- Validation on the latest align example detected one display block covering all four equation lines and produced a good whole-block crop. Local pix2tex still timed out on that crop, so segmentation improved but backend quality/speed remains unresolved.
- Next checkpoint should compare display crop backends: local pix2tex, Sauron/Surya, and possibly an external/math OCR API. Do not add more Tesseract display-math patch rules unless they are very narrow recurring cleanup.

## Custom OCR complex display backend blocker

- User zoomed in and retested equations with subscripts/exponents: `r_1`, `r_2`, `\lambda \pm i\mu`, `c_1 e^{\lambda t}`, and `c_2 t e^{r_1 t}`.
- The zoomed capture improved geometry: processed image was 1596x418 and display crops were visually good enough for a math backend.
- Tesseract still misread prose subscripts/exponents (`r,`, `A + iu`) and display math (`c,e*`, `cytet`), proving this is no longer only a pixel-size issue.
- Forcing local pix2tex on the zoomed display crops timed out on both display blocks, so local pix2tex is not a viable default display backend for these examples.
- Updated auto mode policy: complex display blocks that are not Tesseract-cleanable are marked `unresolved-needs-display-backend` immediately, with a placeholder pointing to `display-crops/block-*.png`, instead of emitting misleading Tesseract fallback or spending 20s on local pix2tex.
- `OCR_CUSTOM_PIX2TEX_MODE=always` still exists for explicit experiments.

## Custom OCR backend mapping pivot

- User clarified the intended key architecture: `Super+N` is local whole-image combined OCR, `Super+B` is Sauron whole-image Surya OCR, and `Super+X` should be the custom split pipeline backed by Sauron rather than the local custom prototype.
- Kept `ocr-custom-split` as the local prototype command for repeatable saved-image testing and local experiments.
- Added `ocr-custom-split-sauron` as the remote profile. It still captures the screen and runs Tesseract/layout segmentation locally, because screen capture is laptop-local and those steps are fast. It sends only complex display-math crops to Sauron's hot OCR API, because display equations are the current bottleneck.
- Documented the model as: local layout + local inline cleanup + remote display backend. This is the smallest pivot that matches the desired `Super+X` behavior without having to port the whole custom splitter into the Sauron service yet.

## Sauron custom display-backend validation

- Built `ocr-custom-split-sauron` successfully and confirmed the wrapper exports `OCR_CUSTOM_BACKEND=sauron` and `OCR_CUSTOM_DISPLAY_BACKEND=sauron`.
- Updated live Hyprland `Super+X` to run `ocr-custom-split-sauron` instead of the local `ocr-custom-split` command.
- Saved-image validation reached Sauron: the wrapper woke Sauron, waited for SSH, confirmed `/health`, called `/warmup`, and posted each `display-crops/block-*.png` to `/ocr/combined`.
- The request path is wired correctly, but Sauron's in-process CUDA Surya path returned HTTP 500 for both display crops with `RuntimeError: CUDA error: unknown error` inside Surya detection (`pixel_values=batch.to(self.model.device)`). This is a Sauron service/GPU backend issue, not a laptop wrapper routing issue.
- Updated the custom splitter so remote API error bodies with `detail` are archived as `display_backend_status=failed:remote-error` rather than being mislabeled as empty OCR output.

## Sauron CPU fallback validation

- User restarted `ocr-api.service` on Sauron after the API patch. `/health` reported `status=ok`, `device_requested=auto`, `device_selected=cpu`, and `device_reason=cuda unavailable; using cpu fallback: torch.cuda.is_available() is false; device_count=1`.
- Retested the latest custom screenshot through `ocr-custom-split-sauron` from the laptop. The request path succeeded end-to-end with Sauron CPU fallback.
- Display crop 1 succeeded in about 16.3s and produced `y = c_1 e^{\lambda t} \cos(\mu t) + c_2 e^{\lambda t} \sin(\mu t).`.
- Display crop 2 succeeded in about 13.0s and produced `y = c_1 e^{r_1 t} + c_2 t e^{r_1 t}.`.
- Remaining accuracy issue is now mostly inline/prose OCR from local Tesseract: the same example still rendered `$r$,`, `$4 +$ iy`, and `$r, = r$` in prose. The custom splitter solved the display equations once Sauron fallback was available, but inline math/subscripts still need a stronger inline backend or better routing.
- Sauron API repo `/opt/ai-services/ocr-api` has an uncommitted `app.py` patch that adds CUDA smoke-testing and CPU fallback before importing Surya settings. Commit and push this repo before considering the service fix durable across machines.

## Custom OCR suspicious inline routing

- Implemented the next checkpoint after fixing Sauron CPU fallback: suspicious inline-math lines in `ocr-custom-split-sauron` now route to Sauron, not just complex display blocks.
- The trigger is intentionally conservative: route inline lines with detected math spans plus evidence of likely Tesseract math failure, such as comma-subscript artifacts (`r,`, `r,,`), low-confidence math tokens, or Greek/complex-number-looking spans like `4 + iy`.
- The remote crop is the whole inline line, not just the small span. This is necessary because isolated crops can be too small for Surya; the full line lets Sauron recover adjacent math tokens such as `$r_1$` and `$r_2$` that Tesseract did not mark as spans.
- Added `line-crops/`, `line-backend/`, and per-line backend status/output fields in `lines.json`.
- Validation on the latest complex-conjugates screenshot produced the intended output:
  `If $r_1$ and $r_2$ are complex conjugates $\lambda \pm i\mu$, then the general solution is`
  display block 1: `$$y = c_1 e^{\lambda t} \cos(\mu t) + c_2 e^{\lambda t} \sin(\mu t).$$`
  `If $r_1 = r_2$, then the general solution is`
  display block 2: `$$y = c_1 e^{r_1 t} + c_2 t e^{r_1 t}.$$`
- Latency is now accuracy-biased and slow on Sauron CPU fallback: inline line 1 ~21.7s, inline line 2 ~12.1s, display block 1 ~14.7s, display block 2 ~12.8s on the saved example.
- Next performance checkpoint should reduce remote calls, likely by batching crops into one Sauron API request or using full-image Surya output as a candidate replacement when it is already good enough.

## Custom OCR display delimiter normalization

- User tested a differential-equation prompt where recognition was good but display formatting was wrong: Sauron returned `$2t^2y'' + 3ty' - y = 0,$\nt > 0,`, and the renderer wrapped that again as a display block, producing triple-dollar output.
- Added display-body normalization before final `$$...$$` wrapping: strip nested inline/display dollar delimiters from display backend output and join simple split condition lines with `\quad`.
- Validation on the saved screenshot now produces:
  `Given that $y_1(t) = t^{-1}$ is a solution of`
  `$$2t^2y'' + 3ty' - y = 0, \quad t > 0,$$`
  `find a fundamental set of solutions.`

## Session wrap-up

- Removed the old local OCR capture archive at `~/.local/share/ocr-captures`; it had 54 attempt directories and occupied about 11M. New OCR attempts will recreate the archive directory as needed.
- Current key mapping at wrap-up: `Super+M` local math OCR, `Super+T` local text OCR, `Super+N` local whole-image Surya combined OCR, `Super+B` Sauron whole-image Surya combined OCR, and `Super+X` custom split OCR with Sauron for suspicious inline math lines and complex display math.
- Current Sauron OCR API state: CUDA is not reliable for PyTorch on the Quadro M5000, so the API smoke-tests CUDA and falls back to CPU before loading Surya. `/health` reports the chosen device and reason.
- Accuracy checkpoint: the custom Sauron split path correctly handled the complex-conjugates example and the differential-equation display delimiter case after inline line routing and display delimiter normalization.
- Performance caveat: Sauron CPU fallback is slow, often 12-22s per remote crop. Next likely performance checkpoint is batching crops into one Sauron request or using full-image Surya output when it is clean enough.
