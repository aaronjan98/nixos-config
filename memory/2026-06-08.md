# 2026-06-08

## OCR archive correction UX

- User live-tested the OCR archive after `nrt`: `~/.local/share/ocr-captures/latest` contained `backend.log`, `input.png`, `metadata.json`, `normalized-output.txt`, and `raw-output.txt`; the first correction helper version created `corrected-output.txt` and `correction-metadata.json`.
- User prefers correcting OCR attempts by editing files directly, including older attempts, rather than relying on a CLI command that only targets the latest attempt.
- Reworked the UX around a per-attempt `review.md` instead of a loose `corrected-output.txt`, because the user wants the screenshot, raw output, normalized output, correction, notes, and status together in one editable file.
- Every new OCR attempt now gets `review.md`; the top-level `~/.local/share/ocr-captures/review.md` is the queue/history file where useful attempts can be kept and irrelevant/good attempts can be removed.
- `ocr-correct-last --from-clipboard` remains as a convenience helper, but it now updates the `## Correction` block inside `review.md`; the primary correction model is direct editing of `~/.local/share/ocr-captures/attempts/<attempt-id>/review.md`.
- Implemented queue entries with sub-bullets linking to the per-attempt `review.md` and `input.png`, plus a notes slot for triage.
- Fixed Markdown fence generation so LaTeX outputs without trailing newlines do not merge into closing code fences.
- Validated with a mock OCR run under `/tmp` and `nix build ./tools#math-ocr --no-link --print-out-paths --option warn-dirty false`.

## Session save — math OCR archive workflow

Saved at: 2026-06-08T08:42:43-07:00

### Worked on

- Finished the first broader OCR feedback checkpoint by making `math-ocr` save reviewable attempt bundles under `~/.local/share/ocr-captures`.
- Added a Markdown-first correction workflow: top-level `review.md` is the queue, and each `attempts/<attempt-id>/review.md` is the editable correction worksheet.
- Updated the live untracked archive README at `~/.local/share/ocr-captures/README.md` so future AJ can find the workflow from inside the archive itself.

### Key decisions

- The source of truth for human corrections is the `## Correction` block in each attempt's `review.md`.
- `ocr-correct-last --from-clipboard` is still useful, but only as a convenience command; direct file editing is the primary workflow.
- `ocr-correct-last --from-clipboard` updates the front matter to `status: corrected`.
- `~/.local/share/ocr-captures` is not currently synced by the multi-machine sync system; Syncthing only covers `~/Documents` and `~/Pictures`, and `sync-machine.sh` covers git repos, dotfiles, pass, and workspace repos.

### Validation

- Mocked `slurp`, `grim`, `pix2tex`, `wl-copy`, `wl-paste`, `notify-send`, and `systemd-run` under `/tmp` to verify archive creation and correction replacement.
- Ran `nix build ./tools#math-ocr --no-link --print-out-paths --option warn-dirty false`; build passed after the final README generator update.

### Next steps

- User should run `nrt`, trigger `math-ocr`, and inspect `~/.local/share/ocr-captures/review.md` plus the linked attempt `review.md`.
- If this workflow feels correct, proceed to the next OCR checkpoint: implement `text-ocr`.
- If cross-laptop OCR examples matter, decide whether to add `~/.local/share/ocr-captures` to Syncthing or keep it intentionally local.

## Text OCR baseline and Surya pivot

- User clarified they still want screen-capture-first OCR, not only PDF/file OCR.
- User is open to newer combined OCR tools such as Surya, but wants pix2tex and Tesseract to remain available as baseline/custom-pipeline components.
- Recommendation: implement `text-ocr` with Tesseract now because it is simple, local, and useful for comparison; do not spend major effort hand-rolling text+math merging until saved examples have been benchmarked against Surya.
- Began Checkpoint 4 by adding `tools/scripts/text-ocr.sh`, a local-only Tesseract wrapper that uses the same `~/.local/share/ocr-captures` archive, queue, `latest` symlink, and editable per-attempt `review.md` workflow as `math-ocr`.
- Clarified archive separation: all OCR types share one root and one queue, but attempt ids are suffixed (`_math`, `_text`, future `_combined`) and metadata/review front matter records `type:`.
- Added per-type latest symlinks to avoid confusion: `latest-math`, `latest-text`, and future `latest-combined`; `latest` remains the newest attempt of any type.
- Final hotkeys after user correction: `Super+M` for `math-ocr`, `Super+T` for `text-ocr`, `Super+N` for `ocr-combined`, `Ctrl+Shift+N` for Neovide, and `Ctrl+Shift+T` for the old Hyprland `togglesplit` action.

## Combined OCR Surya probe

- User confirmed `text-ocr` works much faster than math OCR and asked to proceed with Surya-backed combined OCR.
- Committed the prior checkpoint first as `f39d1fa add text ocr baseline and combined placeholder`.
- Surya v2 is a better fit for a probe than Pix2Text because it accepts image paths, emits layout-aware block output, and handles math inline with `<math>...</math>` tags.
- Important Surya runtime note: Surya v2 requires an inference backend (`llama.cpp` for local CPU/Apple Silicon or `vllm` for NVIDIA GPU). This means the command may be much slower/heavier than Tesseract.
- Implemented `tools/scripts/bootstrap-surya-ocr.sh` to create `~/.local/share/ocr-runtimes/surya/.venv` and install `surya-ocr==0.16.0`.
- Replaced the `ocr-combined` placeholder with a Surya adapter that captures a screen region, creates a `_combined` archive attempt, runs `surya_ocr input.png --output_dir <attempt>/surya`, extracts block `html` into `normalized-output.txt`, copies it to clipboard, and writes the same editable `review.md`.
- Added `latest-combined` symlink support.
- Mock validation passed with a fake `surya_ocr`; live validation is pending `nrt`, `bootstrap-surya-ocr`, and a real `Super+N` capture.
- First live `Super+N` attempt did not hang; it spent about 88 seconds downloading Surya models and processing, then failed in the wrapper because Surya wrote results to `surya/input/results.json` while the wrapper expected `surya/results.json`.
- Patched `ocr-combined` to discover nested Surya `results.json`, copy it to canonical `surya/results.json`, and normalize both Surya `blocks[].html` and `text_lines[].text` outputs.
- Patched `bootstrap-surya-ocr` to export `LD_LIBRARY_PATH` from `SURYA_EXTRA_LIBRARY_PATH`; direct Surya imports needed Nix-provided `libstdc++.so.6` during verification.
- User reported Surya recognized the text+math capture poorly (`v'` instead of `y'`, line order wrong) and returned math as `<math>...</math>` tags. Patched the wrapper's normalizer to convert `<math>...</math>` to Markdown inline math `$...$`; recognition mistakes remain a model/capture-quality issue, not a formatting issue.

## Session save — combined OCR warm runtime

Saved at: 2026-06-08T14:12:05-07:00

### Worked on

- Committed the Surya combined OCR checkpoint as `ce9869a add surya combined ocr probe`.
- User confirmed `ocr-combined` now works after `nrt`.
- Discussed why local Surya feels slow: Python/venv imports and model/runtime startup happen before visible CPU load, then inference runs on laptop CPU and may become memory-bandwidth-bound rather than using every core.
- Clarified why Mathpix can be faster despite being remote: their models are already warm on optimized server hardware, while the laptop command pays local startup and CPU inference costs.

### Decisions

- Next checkpoint is warm runtime benchmarking before building a custom text+math splitter.
- Keep Surya as the current `ocr-combined` backend, but add tooling so it can keep its inference server warm between captures.
- Compare local cold, local warm, and future `sauron` warm service before deciding whether remote execution or custom integration is worth the added complexity.

### Open questions

- Whether Surya's current `--keep_server` behavior is enough for fast repeated captures, or whether a dedicated user service is needed.
- Whether `sauron` has enough CPU/GPU advantage to beat local warm Surya after accounting for screenshot upload and network latency.

### Next steps

- Add explicit warm-mode commands or documentation for `ocr-combined`.
- Record timing metadata clearly enough to compare cold versus warm runs.
- After local warm testing, decide whether to implement an `OCR_BACKEND=sauron` API.

### Implementation update

- Found a version mismatch: the mutable venv was pinned to `surya-ocr==0.16.0`, whose installed CLI does not expose `--keep_server`.
- Verified from upstream Surya/PyPI docs that `surya-ocr==0.20.0` is the Surya 2 release and includes `--keep_server`, `SURYA_INFERENCE_KEEP_ALIVE`, and the shared `llama.cpp`/`vllm` inference backend model.
- Updated the bootstrap default to `surya-ocr==0.20.0` and made bootstrap verify that `surya_ocr --help` exposes `--keep_server`.
- Added `ocr-combined-warm` as the explicit screen-capture command that keeps the Surya inference server alive.
- Added `ocr-combined-warmup` as the no-capture warmup command that generates a tiny image, starts Surya with `--keep_server`, and leaves the server ready for later captures.
- Added metadata fields for keep-alive and inference backend settings so cold/warm/local/remote timing comparisons can be made from attempt metadata.

### Debug update after live warmup confusion

- `Super+N` was still bound to `ocr-combined` in `/home/aj/.config/hypr/conf.d/20-binds.conf`, not `ocr-combined-warm`.
- Clarified command roles: `ocr-combined-warmup` is not a capture command; it only primes the Surya backend, while `ocr-combined-warm` is the hotkey-compatible capture command.
- Live `ocr-combined-warmup` looked like a hang because it prints no useful progress before Surya import/model startup; the user interrupted it during Torch import.
- A following `Super+N` run failed after about 137 seconds because Surya's spawn lock at `~/.cache/datalab/surya/llamacpp_server.lock` could not be acquired.
- Surya's default `SURYA_INFERENCE_PARALLEL=8` caused `llama-server ctx-size=98304`, which is inappropriate for a single laptop screenshot and likely increases startup/memory pressure.
- Patched local wrappers to default to single-capture CPU settings: `SURYA_INFERENCE_BACKEND=llamacpp`, `SURYA_INFERENCE_PARALLEL=1`, `SURYA_INFERENCE_CTX_SIZE=16384`, and `SURYA_INFERENCE_LOGPROBS=false`.
- Added `ocr-combined-stop` to stop the warm server and clear the Surya lock when a warmup attempt gets wedged.
- Added visible startup output/notification to `ocr-combined-warmup` so it no longer appears completely inert.

### Warm mode block and rollback

- User ran `ocr-combined-warmup`; it printed startup text but did not complete.
- Logs showed Surya 2 did spawn `llama-server` with the reduced settings, but `llama-server` failed while loading `surya-2.gguf` with `unknown model architecture: 'qwen35'`.
- This means the problem is not just slow CPU startup; the current Nix `llama-cpp` package is too old or otherwise lacks the Surya 2 GGUF architecture support required for local warm mode.
- Cleared the stale Surya sentinel and lock with `ocr-combined-stop`.
- Restored the supported local Surya runtime pin to `surya-ocr==0.16.0` so `Super+N` can return to the previously working standard `ocr-combined` path.
- Removed `ocr-combined-warm` and `ocr-combined-warmup` from the installed toolset for now to avoid commands that look valid but cannot work with the current `llama-server`.
- Remaining decision: either package a newer `llama.cpp` just for Surya 2, or skip local warm mode and implement a warm remote backend on `sauron`.

## Remote OCR on sauron

- User chose to implement combined OCR on Sauron instead of continuing local Surya 2 warmup.
- Reviewed Sauron docs: HP Z640, 28 cores / 56 threads, 125 GB RAM, Quadro M5000, WOL via `wol-sauron`, SSH aliases `sauron` and `sauron.home`, auto-suspend after 30 minutes idle.
- Reviewed video-summary workflow: laptop script wakes Sauron, waits for SSH, health-checks `summarizer-api`, then calls a FastAPI endpoint through SSH/curl.
- Existing summarizer API lives at `/opt/ai-services/summarizer-api`, runs as user `aj`, uses a venv in-place, and is launched by systemd.
- Decision: create a separate `/opt/ai-services/ocr-api` instead of extending `summarizer-api`, because OCR has different dependencies and screenshots should not be coupled to video summarization.
- First remote OCR slice should use `surya-ocr==0.16.0` on Sauron for a working baseline; Surya 2 remains blocked until a compatible `llama.cpp` backend exists.
- Laptop `ocr-combined` should gain `OCR_BACKEND=sauron`: capture locally, archive locally, wake Sauron, wait for SSH, POST the screenshot to Sauron's local OCR API, then copy/archive the returned text exactly like the local backend.
- Implemented `/opt/ai-services/ocr-api` on Sauron with FastAPI, `surya-ocr==0.16.0`, and endpoint `POST /ocr/combined`.
- User installed `/etc/systemd/system/ocr-api.service`; service is enabled and active as a system service on `127.0.0.1:8011`.
- Created Forgejo repo `aj/sauron-ocr-api` and wired Sauron's local repo remote `home` to `sweetpea-git:/srv/git/repos/sauron-ocr-api.git`; push succeeded and Forgejo sync worked.
- Verified `OCR_BACKEND=sauron` laptop wrapper path with a saved screenshot; end-to-end remote request succeeded and copied normalized output.
- Added the explicit `ocr-combined-sauron` package wrapper so `Super+N` can stay local and `Super+B` can call Sauron for direct speed comparison.
- Clarified architecture: Sauron is hot as a running API service while awake, but the current Surya 0.16 CLI backend still loads the OCR model per request. It is not Surya 2 and not yet an in-memory predictor daemon.
- Sauron auto-suspend remains compatible with the laptop wrapper because the command sends `wol-sauron`, waits for SSH, then calls the API through SSH; a sleeping Sauron adds wake latency to the first request.

### Sauron hot in-process OCR implementation

- Replaced the Sauron OCR API subprocess path with an in-process Surya 0.16 engine that loads `FoundationPredictor`, `DetectionPredictor`, and `RecognitionPredictor` once and reuses them for requests.
- Added `GET/POST /warmup` on Sauron and enabled startup preload by default in `app.py`, so the model becomes resident while `ocr-api.service` is alive.
- Verified `/health` reports `mode: in-process`, `model.loaded: true`, and `load_duration_ms` around 4606 ms after restart.
- Verified two direct hot `/ocr/combined` requests through SSH on the saved combined screenshot: server durations were about 29.9s and 27.0s, with the model staying loaded.
- Updated the laptop `ocr-combined` wrapper so `OCR_BACKEND=sauron` calls `/warmup` before `POST /ocr/combined` when `SAURON_WARMUP=1` (default).
- Validated the packaged `ocr-combined-sauron` command with a mocked local capture and the live Sauron API; the attempt succeeded and recorded `sauron_warmup: 1` in metadata.
- Sauron is still CPU-only for this service: Torch reports CUDA unavailable because the installed Torch CUDA build expects a newer NVIDIA driver than Sauron's current driver.

### Sauron GPU and OCR checkpoint reassessment

- User challenged the next checkpoint: instead of only benchmarking local vs Sauron CPU OCR, the stronger next checkpoint may be GPU enablement/custom integration on Sauron.
- Reviewed zettelkasten notes on Sauron hardware and GPU plans: `Outside/Clippings/Graphic Cards for AI.md`, `Zettels/Tech/Power Consumption of the HP Z640 Tower Workstation.md`, `Inside/Scratch/About Quadro M5000.md`, `Inside/Scratch/upgrading the workstation specs.md`, and Sauron/Kubernetes homelab notes.
- Current Sauron OCR service is already a custom in-process Surya integration, but Surya reports `TORCH_DEVICE_MODEL=cpu`; Torch sees one CUDA device but `torch.cuda.is_available()` is false because the installed Torch CUDA build expects a newer NVIDIA driver than the current Sauron driver.
- Conclusion: custom integration alone does not enable GPU. The next meaningful speed checkpoint is to make PyTorch CUDA work on Sauron, then rerun the same in-process OCR service with Surya on CUDA.
- User's prior GPU planning emphasized VRAM-first choices: RTX 3060 12GB as budget entry, RTX 3090 24GB as value/sweet spot, RTX A4000/A6000 as workstation-class options, and caution around AMD/ROCm complexity.

### Sauron Quadro CUDA test

- Sauron is Ubuntu 22.04.5 with `nvidia-driver-535` and Quadro M5000. `nvidia-smi` reports driver 535.309.01 and CUDA 12.2 support.
- Production OCR venv has `torch 2.12.0+cu130`, so CUDA initialization fails because driver 535 is too old for CUDA 13.
- A temporary test venv with `torch==2.4.1+cu121` proved the Quadro works: `torch.cuda.is_available()` was true, device was `Quadro M5000`, compute capability `(5, 2)`, and a CUDA matmul succeeded.
- Surya 0.16 could be manually installed with that older Torch, but full OCR failed because Surya calls `torch.nn.utils.rnn.pad_sequence(..., padding_side=...)`, which is not available in Torch 2.4.1.
- Cleaned up both failed temporary test venvs. Recommended next path: upgrade Sauron's NVIDIA driver to a current Ubuntu-supported driver such as `nvidia-driver-580`, reboot, then test whether the production `torch 2.12.0+cu130` can initialize CUDA on the M5000. If CUDA 13/PyTorch no longer supports Maxwell well enough, fall back to a Torch 2.7+ CUDA 12.x wheel or an older Surya/Torch compatibility path.
- User installed `nvidia-driver-580` and rebooted Sauron. CUDA 13 initialized, but PyTorch 2.12 still failed real GPU execution with `cudaErrorNoKernelImageForDevice` because its wheel supports `sm_75+` and the Quadro M5000 is `sm_52`.
- Tested `torch==2.7.1+cu126`: CUDA matmul worked on the Quadro, PyTorch exposed `pad_sequence(..., padding_side=...)`, and full Surya OCR succeeded.
- Promoted the CUDA 12.6 test venv to production `.venv`, added Sauron `local.env` with `TORCH_DEVICE=cuda`, `DETECTOR_BATCH_SIZE=4`, `RECOGNITION_BATCH_SIZE=16`, and `DISABLE_TQDM=true`, and repaired moved-venv console script shebangs.
- Sauron OCR API health now reports loaded in-process Surya on CUDA. A direct remote OCR request on the saved screenshot took about 22.5s server-side versus the previous CPU range around 27-30s.
- Updated Sauron OCR API `requirements.txt` to pin `torch==2.7.1+cu126` using the PyTorch CUDA 12.6 wheel index, and updated Sauron's README to document why CUDA 12.6 is intentional for the Quadro M5000.
