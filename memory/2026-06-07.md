# 2026-06-07

## math-ocr / pix2tex planning

- User clarified the pix2tex runtime should live at `~/Repositories/automation/pix2tex`, not `~/Repositories/self-hosted/pix2tex`.
- User created the sweetpea bare repo at `/srv/git/repos/pix2tex.git`, populated local `~/Repositories/automation/pix2tex` from `lukas-blecher/LaTeX-OCR`, pushed `main` and tags to `home`, and disabled pushes to the `upstream` GitHub remote.
- Verified local pix2tex repo state: branch `main` tracks `home/main`; `home` points to `ssh://git@ssh.aaronjanovitch.com:2222/srv/git/repos/pix2tex.git`; `upstream` fetches `https://github.com/lukas-blecher/LaTeX-OCR.git` and has push URL `DISABLED`.
- Updated `project-memory/math-ocr-pix2tex-venv-spec.md` and `tools/scripts/bootstrap-pix2tex.sh` to use `~/Repositories/automation/pix2tex` as the runtime path.
- Completed Checkpoint 3: `bootstrap-pix2tex` now builds and manually creates/repairs `~/Repositories/automation/pix2tex/.venv`.
- First bootstrap attempt exposed dependency drift: unbounded PyPI resolution pulled newer dependencies that compiled `stringzilla` for several minutes. Stopped that run and added constraints in `tools/scripts/bootstrap-pix2tex.sh` (`albumentations==1.3.1`, `transformers<5.0`, `huggingface-hub<1.0`, `tokenizers<0.22`, plus NumPy/Pandas/Pydantic caps).
- Rebuilt `./tools#math-ocr`, reran bootstrap, and verified: `pix2tex` imports, `.venv/bin/pix2tex --help` works, `.venv/` is ignored in `.git/info/exclude`, and `.venv/.pix2tex-bootstrap-rev` matches `5c1ac929bd19a7ecf86d5fb8d94771c8969fcb80`.
