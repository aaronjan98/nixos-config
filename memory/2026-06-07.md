# 2026-06-07

## math-ocr / pix2tex planning

- User clarified the pix2tex runtime should live at `~/Repositories/automation/pix2tex`, not `~/Repositories/self-hosted/pix2tex`.
- User created the sweetpea bare repo at `/srv/git/repos/pix2tex.git`, populated local `~/Repositories/automation/pix2tex` from `lukas-blecher/LaTeX-OCR`, pushed `main` and tags to `home`, and disabled pushes to the `upstream` GitHub remote.
- Verified local pix2tex repo state: branch `main` tracks `home/main`; `home` points to `ssh://git@ssh.aaronjanovitch.com:2222/srv/git/repos/pix2tex.git`; `upstream` fetches `https://github.com/lukas-blecher/LaTeX-OCR.git` and has push URL `DISABLED`.
- Updated `project-memory/math-ocr-pix2tex-venv-spec.md` and `tools/scripts/bootstrap-pix2tex.sh` to use `~/Repositories/automation/pix2tex` as the runtime path.
- Completed Checkpoint 3: `bootstrap-pix2tex` now builds and manually creates/repairs `~/Repositories/automation/pix2tex/.venv`.
- First bootstrap attempt exposed dependency drift: unbounded PyPI resolution pulled newer dependencies that compiled `stringzilla` for several minutes. Stopped that run and added constraints in `tools/scripts/bootstrap-pix2tex.sh` (`albumentations==1.3.1`, `transformers<5.0`, `huggingface-hub<1.0`, `tokenizers<0.22`, plus NumPy/Pandas/Pydantic caps).
- Rebuilt `./tools#math-ocr`, reran bootstrap, and verified: `pix2tex` imports, `.venv/bin/pix2tex --help` works, `.venv/` is ignored in `.git/info/exclude`, and `.venv/.pix2tex-bootstrap-rev` matches `5c1ac929bd19a7ecf86d5fb8d94771c8969fcb80`.
- User asked to commit the Checkpoint 3 work plus unrelated dirty files; committed all current dirty files on branch `math-ocr` as `30f6fbb` with message `add pix2tex bootstrap runtime`.
- Completed Checkpoint 4: updated `tools/scripts/math-ocr.sh` to call `~/Repositories/automation/pix2tex/.venv/bin/pix2tex` (or `pix2tex_cli`) directly, fail clearly with a `bootstrap-pix2tex` instruction if the venv is missing, and pass `--no-cuda` for CPU execution.
- Verified Checkpoint 4 with `nix build ./tools#math-ocr --no-link --print-out-paths --option warn-dirty false`, built wrapper inspection, `.venv/bin/pix2tex --help`, and `import pix2tex`. Full interactive screenshot OCR remains Checkpoint 5.
- Checkpoint 5 manual OCR test now passes with the newly built wrapper: pix2tex runs from `~/Repositories/automation/pix2tex/.venv`, OCR output is copied as formula-only text, and the user verified `wl-paste` from their shell returned `{\frac{f(a+\Delta x)-f(a)}{\Delta x}}.`.
- Fixed clipboard lifetime by launching `wl-copy --foreground` through `systemd-run --user`; direct `wl-copy` from a short-lived agent command did not persist without a clipboard manager.
- Added LaTeX output normalization so a redundant whole-output group like `{\frac{...}{...}}` is stripped while preserving inner braces such as `{x}_{i}`.
- `ALT+M` still fails because Hyprland executes `/run/current-system/sw/bin/math-ocr`, which resolves to the previous installed wrapper that searches `PATH` for pix2tex. The new wrapper is built but not installed into the current system generation until the rebuild/switch checkpoint.

## math-ocr / pix2tex session save

Saved at: 2026-06-07T23:04:38-07:00

### Worked on

- Completed the pix2tex runtime slice of the math OCR project: `math-ocr` captures a region, runs `~/Repositories/automation/pix2tex/.venv/bin/pix2tex`, normalizes output, and copies formula-only LaTeX to the Wayland clipboard.
- Fixed `ImportError: libstdc++.so.6` by exporting a Nix-provided `PIX2TEX_EXTRA_LIBRARY_PATH` into `LD_LIBRARY_PATH` for the venv runtime.
- Fixed clipboard persistence by launching `wl-copy --foreground` through `systemd-run --user`; direct short-lived `wl-copy` did not persist without a running clipboard manager.
- Changed the Hyprland binding from `ALT+M` to `Super+M`; user ran `nrt`, and the hotkey reached the new wrapper.

### Key insights

- `~/nixos-config/project-memory/math-ocr-pix2tex-venv-spec.md` covers only the pix2tex/math formula OCR slice, not the whole Mathpix-like text+math OCR project.
- The broader project appears in zettelkasten notes around Pix2Text as a Mathpix alternative: `Staging/free alternatives to Mathpix.md`, `Staging/retrain or fine-tune Pix2Text.md`, and `Inside/Paper/AI responses/Would Training or Refining Pix2Text Be a Good ML Portfolio Project?.md`.
- `Inside/Projects/Install OCR on nix.md` links only to the pix2tex attempt notes and does not contain the full text+math OCR spec.
- pix2tex quality is model/input-bound: the wrapper can work mechanically while the model still produces poor LaTeX for complex equations.

### Decisions

- Do not retry pure-Nix pix2tex packaging.
- Keep pix2tex as a mutable local repo + venv runtime under `~/Repositories/automation/pix2tex`.
- Treat the next phase as a broader OCR pipeline decision, likely involving Pix2Text or another layout/text/math system rather than only pix2tex.

### Open questions

- Whether to build the next phase around Pix2Text as the all-in-one text/layout/math OCR engine, or keep separate engines for text OCR and math OCR and merge their outputs.
- Whether to add a feedback archive/correction command before attempting fine-tuning.
- Whether to add a warm daemon for speed, since repeatedly starting pix2tex reloads the model.

### Next steps

- Commit the current NixOS changes.
- Decide whether to write a new project-memory spec for the broader text+math OCR pipeline.
- If continuing pix2tex quality work, add an attempt archive and correction capture before any model fine-tuning.
- If continuing system integration, run `nrs` only when the user wants this test generation promoted to the boot generation.
- Wrote the broader OCR architecture spec at `project-memory/text-math-ocr-pipeline-spec.md`. It separates `math-ocr`, `text-ocr`, and `ocr-combined`, and recommends implementing an OCR attempt archive plus correction commands before training or changing engines.
- Linked the broader OCR spec from `CONTEXT.md` under active project specs.
- The bigger OCR plan is now explicitly distinct from `project-memory/math-ocr-pix2tex-venv-spec.md`: pix2tex is only the formula-only slice, while the broader target is Mathpix-like text + layout + math to Markdown.
- Recommended next checkpoint for the broader OCR work: add attempt bundles under `~/.local/share/ocr-captures` and `ocr-correct-last --from-clipboard` so bad math OCR outputs become training/evaluation data.
- Implemented the first broader OCR checkpoint in `math-ocr`: each run now creates an attempt bundle under `~/.local/share/ocr-captures/attempts`, updates `~/.local/share/ocr-captures/latest`, and saves `input.png`, `raw-output.txt`, `normalized-output.txt`, `backend.log`, and `metadata.json`.
- Added `tools/scripts/ocr-correct-last.sh` and exposed it through `tools/pkgs/math-ocr.nix`; `ocr-correct-last --from-clipboard` writes the clipboard correction into `corrected-output.txt` plus `correction-metadata.json` for the latest attempt.
- Mock validation passed using fake `slurp`, `grim`, `pix2tex`, `wl-copy`, and `wl-paste`; validated archive creation, normalization, metadata, copied output, and correction capture without invoking real screen capture or pix2tex.
- `nix build ./tools#math-ocr --no-link --print-out-paths --option warn-dirty false` passed after staging the new correction script so the flake source could see it. Live use still requires `nrt`/`nrs` to install the updated commands.
