# Session Notes — 2026-07-07

## Nix rebuild command preference

- User prefers to run `nrs` and other long NixOS build/apply commands themselves so they can see live output directly.
- Agent should prepare fixes, explain the expected command, and ask/tell the user to run it rather than running `nrs` or equivalent long system builds in the tool session.
