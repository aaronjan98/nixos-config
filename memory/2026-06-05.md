# 2026-06-05

## math-ocr / pix2tex planning

- User confirmed the runtime/venv mental model mostly makes sense and approved `~/Repositories/tools/pix2tex` as the intended runtime path.
- Refined the runtime layout after checking `export-workspace-state.sh`: `~/Repositories/tools/pix2tex` should be the pix2tex git clone itself with an ignored `.venv/` inside it, not a non-repo parent containing `repo/` and `.venv/` siblings.
- Clarified that `.venv` should not be committed into `~/nixos-config` or the pix2tex repo; only scripts, pins, requirements, routing docs, and bootstrap instructions should be tracked.
- Checked existing repo patterns: update scripts mutate tracked Nix derivation files and then require `nrs`; workspace bootstrap scripts recreate untracked local repos from tracked manifests; multi-machine sync explicitly excludes `.venv`, caches, `node_modules`, and other repo-local runtime artifacts.
- Noted that using `~/Repositories/tools/pix2tex` requires adding a new top-level `tools` area to the `~/Repositories` routing layer, because the current router has no `tools` area.
- User later agreed `~/Repositories/self-hosted/pix2tex` is a better runtime path than creating a new top-level `tools` area; updated the spec accordingly.
- Completed Checkpoint 1 cleanup: deleted abandoned pure-Nix pix2tex files/directories and removed stale commented references from `flake.nix` and `hosts/common/default.nix`. No bootstrap logic or build verification was added yet.
- Committed Checkpoint 1 cleanup on branch `math-ocr` as `9e42e5a` with message `clean up math ocr vestiges`.
- Completed Checkpoint 2 build verification: `nix build ./tools#math-ocr --no-link --option warn-dirty false` passed. The first sandboxed run failed only because Nix could not write `~/.cache/nix/fetcher-cache-v4.sqlite`; rerunning with escalation succeeded.
- Started Checkpoint 3: added `tools/scripts/bootstrap-pix2tex.sh` and refactored `tools/pkgs/math-ocr.nix` so the `math-ocr` tools package exposes both `math-ocr` and `bootstrap-pix2tex`.
- Verified the updated tools package builds with `nix build ./tools#math-ocr --no-link --option warn-dirty false`.
- Manual `bootstrap-pix2tex` run is blocked because the expected local git server repo `ssh://git@ssh.aaronjanovitch.com:2222/srv/git/repos/pix2tex.git` does not exist. Also checked `LaTeX-OCR.git` and `latex-ocr.git`; neither exists.
- Patched `bootstrap-pix2tex` to report this missing-source condition clearly and mention the `PIX2TEX_REPO_URL=<url>` override.
