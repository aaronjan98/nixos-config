# 2026-07-12 Ian Asher - Take Me (To The Moon) YouTube rip

## What was done
- Checked Navidrome via Subsonic `search3` for "Ian Asher" / "Take Me" — not in the library (only unrelated "Take Me" matches: Korn, Dixie Chicks, Roxy Music).
- Ripped from YouTube per the established workflow (`Raymer/memory/2026-06-24 youtube-rips-and-metadata-fragmentation.md`):
  - Source: `https://www.youtube.com/watch?v=tNHXDNaNqsU` — "Ian Asher - Take Me (To The Moon) (Official Audio)", uploaded 2026-06-05, 159s.
  - `nix run nixpkgs#yt-dlp -- -f 140 -o /tmp/yt_raw.m4a <url>`, scp'd to qwerty, tagged via `python:3-slim` Docker container (mutagen) and placed as `aj` couldn't write directly (files are `114:121`).
  - Placed at `/mnt/storage/music/Non-Album/Ian Asher/Take Me (To The Moon).m4a`, tags: artist/albumartist "Ian Asher", album "Singles", year 2026 (upload year), `OWNER=AJ` freeform atom set.
- Triggered `docker exec navidrome navidrome scan --full` on qwerty; import confirmed via `search3` — new artist `Ian Asher` (id `00zllg5STdAOUNFTCvpSM2`), album `Singles` (id `3GmK0s4z6zX56qTNn0qyk6`), song id `eCPK3GnKVS17ZBVVF8pzUN`.

## Navidrome auth note
- Password file path referenced in prior notes (`~/ipod-rockbox/.aj-navidrome-pass`) does not exist on the laptop; had to read it from qwerty (`/opt/homelab/navidrome/.aj-navidrome-pass`) over SSH instead.

## Anomaly (not investigated further)
- Immediately after the scan, the new song's `search3` record showed `playCount:2` and a `starred` timestamp a few minutes after `created` — despite being a brand-new file nobody had knowingly played yet. Not treated as a security concern (just JSON data, no embedded instructions), but worth checking if some client on the LAN auto-plays/stars newly-added tracks, since that wasn't expected.

## Follow-up: batch rip of recommended Ian Asher tracks
- User asked for recommendations based on liking "Take Me (To The Moon)", then asked to grab all of them. Used WebSearch to confirm exact official-audio YouTube links (avoided guessing URLs) since several are remixes/collabs and title-matching mattered.
- Ripped 7 more tracks via the same yt-dlp -f 140 -> scp -> mutagen-in-Docker -> full Navidrome scan workflow, all placed under `/mnt/storage/music/Non-Album/Ian Asher/`, album tag "Singles", albumartist "Ian Asher", owned `114:121`:
  - `Runaway (U & I)` (feat. Galantis) — yt `j-6PIWr_saA`, 2025
  - `Constellations` (feat. Jade LeMac) — yt `IrpAOQT-JqA`, 2025
  - `Jenny (I Wanna Ruin Our Friendship)` (feat. Studio Killers) — yt `AXtWV2TWlfw`, 2025
  - `Desire` (feat. Olly Alexander/Years & Years) — yt `ZDw_x_REei0`, 2025
  - `Way Too Self Aware (Remix)` (orig. Temper City "Self Aware") — yt `LDmiy47h-Nk`, 2026
  - `Black Out Days (Stay Away)` (feat. Phantogram) — yt `FFxsTiFGWT8`, 2025
  - `Colors (Ian Asher Remix)` (orig. Halsey) — yt `yIQj0hq_Acc`, 2025
- Verified all 8 Ian Asher tracks (incl. original "Take Me (To The Moon)") present via Subsonic `search3` after the full scan.
- Library now has an `Ian Asher / Singles` album with 8 tracks — a full album/artist page (cover art, artist image) hasn't been set up like the TAA playlists get; only individual track rips.
