{
  tmux = {
    # Lighter than colour21 (deep blue → brighter blue)
    darkerFg = "colour27";

    # Lighter than colour124 (dark red → brighter red)
    accentFg = "colour160";

    cursor = "#FF2800";

    # Lighter than "purple" (use a lighter purple from 256-color palette)
    lighterFg = "colour141";

    defaultBg = "default";

    # Lighter than #13040f but same “wine/purple” highlight vibe
    bgHighlight = "#2a0a22";
  };
}
